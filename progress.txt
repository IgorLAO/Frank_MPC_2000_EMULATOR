## 2026-04-03 - US-011
- What was implemented: `LoopRecorder` struct in `src/loop_recorder.rs` — records `(pad_index, elapsed_ms)` events using `std::time::Instant` for timing. `start()` begins capture, `stop()` finalizes a named `Loop` (loop#1, loop#2, ...) and pushes to `self.loops`. `record_event(pad_idx)` called from `main.rs` whenever a pad is triggered. L key and "Loop Rec (L)" button toggle loop recording. Blinking blue indicator shown while loop recording is active. Events recorded for both keyboard and mouse pad triggers.
- Files changed: src/loop_recorder.rs (new), src/main.rs
- **Learnings for future iterations:**
  - `std::time::Instant` used for loop event timing (accurate relative elapsed_ms per loop)
  - `loop_recorder.record_event(pad_idx)` called before `trigger_pad(pad_idx)` in the triggered loop
  - L key has no conflict with existing key mappings (Q/W/E/A/S/D/F/Z/X/C/V/1/2/3/4/R/Space)
  - `self.loops` Vec holds all completed loops; index+1 gives sequential loop number
  - Fields/methods are pub to allow access from main.rs for upcoming US-012/013/014
---

## 2026-04-03 - US-010
- What was implemented: Long-press pad 3 seconds to clear its assigned sample. `pad_press_start: [Option<f64>; 16]` tracks press start time. `Sense::click_and_drag()` used instead of `Sense::click()` to detect ongoing pointer-down state via `response.is_pointer_button_down_on()`. Red progress bar fills at bottom of pad over 3s. Border/bg shift to red while holding. On completion, sample and sink are cleared; pad returns to empty state. Short clicks (<3s) still trigger normal behavior.
- Files changed: src/main.rs
- **Learnings for future iterations:**
  - `Sense::click_and_drag()` needed (not `Sense::click()`) to get `is_pointer_button_down_on()` to work
  - `response.is_pointer_button_down_on()` returns true while primary button is held over the widget
  - Re-compute `has_sample` after long-press clearing (moved it after the clear block)
  - `long_press_complete` guard on `response.clicked()` prevents accidental pad trigger after 3s hold
  - `pad_press_start[idx] = None` must be set after clearing to reset progress bar next frame
---

## 2026-04-03 - US-009
- What was implemented: Record button ("⏺ Record (R)") in UI toolbar toggles recording start/stop. R key also toggles. Blinking red dot indicator shown while recording. After stopping, a yellow hint message prompts user to click a pad to assign the buffer. Clicking a pad during pending-assign mode stores the WAV buffer and shows a filled dot (●) in the pad label. Pads with pending assignment show a blue border highlight.
- Files changed: src/main.rs
- **Learnings for future iterations:**
  - R key conflict with US-007 pad mapping — R was removed from pad triggers and reassigned to recording toggle
  - `ctx.input(|i| i.time)` gives elapsed seconds as f64; use `(t * 2.0) as u64 % 2 == 0` for 0.5 Hz blink
  - `self.pending_record_buffer.take()` cleanly clears the pending state when assigned
  - Pad 3 (index 3) now has "-" in PAD_KEY_LABELS since R is taken by recording
  - encode_wav returns empty Vec<u8> if no samples captured — check `!wav.is_empty()` before setting pending buffer
---

## 2026-04-03 - US-008
- What was implemented: RecordingEngine in src/recording.rs — starts/stops cpal default input capture, collects f32 samples in Arc<Mutex<Vec<f32>>>, encodes to WAV (16-bit PCM) on stop(). Added `recorder: RecordingEngine` field to MpcApp.
- Files changed: src/recording.rs (new), src/main.rs
- **Learnings for future iterations:**
  - cpal 0.15 `build_input_stream` signature: `(config: &StreamConfig, data_cb, err_cb, timeout: Option<Duration>)`
  - `SupportedStreamConfig::config()` gives the `StreamConfig` needed for the stream builder
  - `SampleFormat` is non-exhaustive; match F32/I16/U16 with `_ => false` fallback
  - `RecordingEngine._stream = None` dropping the Stream halts capture immediately
  - `is_recording()` method uses `Arc<AtomicBool>` readable from any thread
  - `cpal::Stream` is not Send on all platforms; keep it on the main thread
---

## 2026-04-03 - US-007
- What was implemented: Keyboard keys Q/W/E/R (pads 1-4), A/S/D/F (5-8), Z/X/C/V (9-12), 1/2/3/4 (13-16) now trigger pads. Key labels shown on pads above the pad number.
- Files changed: src/main.rs
- **Learnings for future iterations:**
  - Use `egui::Key::Q` etc. and `egui::Key::Num1` through `egui::Key::Num4` for digit keys
  - Collect key presses inside `ctx.input(|i| { ... })` closure; push to triggered Vec to avoid borrow issues
  - `PAD_KEY_LABELS` const array defined outside impl block for pad display labels
---

## Codebase Patterns
- Rust 2024 edition with eframe 0.31, egui 0.31, rodio 0.20, cpal 0.15
- Build requires: pkg-config, libasound2-dev, build-essential (gcc)
- Use `cargo check` for typecheck, `cargo build` to compile
- eframe::run_native takes a closure returning `Ok(Box::new(App))`
- egui::CentralPanel::default().show() for main UI layout

---

## 2026-04-03 - US-006
- What was implemented: Stop All button (red, "■ Stop All") added to UI above the pad grid; Space key also triggers stop_all; stop_all() method stops all pad sinks immediately; pad active states reset on next repaint since stopped sinks become empty.
- Files changed: src/main.rs
- **Learnings for future iterations:**
  - Use `ctx.input(|i| i.key_pressed(egui::Key::Space))` for Space key detection in egui
  - `egui::Button::new(...).fill(color).min_size(size)` for styled buttons
  - `sink.stop()` immediately stops a rodio Sink; `sink.empty()` returns true after stop so visual state updates automatically on next frame
---

## 2026-04-03 - US-001
- What was implemented: Initialized Rust project with Cargo.toml including eframe, egui, rodio, cpal. Created basic eframe window with MpcApp struct implementing eframe::App.
- Files changed: Cargo.toml, src/main.rs, Cargo.lock
- **Learnings for future iterations:**
  - System requires: apt install build-essential pkg-config libasound2-dev
  - eframe 0.31 uses `eframe::run_native` with closure returning `Ok(Box::new(...))`
  - Rust 2024 edition is available (Rust 1.94.1)
  - `cargo check` passes in ~20s after first full compile
---
